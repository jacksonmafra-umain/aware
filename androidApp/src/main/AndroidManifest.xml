<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- KSensor LOCATION sensor/state (Trip tracker). The library can request these at runtime
         via KSensor.AskPermission(PermissionType.LOCATION); they must still be declared here. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- KSensor CONNECTIVITY / ACTIVE_NETWORK states (Smart download). registerNetworkCallback
         throws SecurityException without these; they are install-time (no runtime prompt). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- KSensor BLE_CONNECTIONS / BLE_DISCOVERS states (Nearby devices). BLUETOOTH_SCAN/CONNECT are
         runtime permissions on API 31+; if not granted the source layer reports a handled error
         rather than crashing. Legacy permissions cover API <= 30. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- KSensor STEP_COUNTER / STEP_DETECTOR (Daily step goal). Runtime permission on API 29+;
         requested at launch in MainActivity. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <application
        android:name=".AwareApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>